# 🖥️ 프론트엔드 스펙 (Frontend Specification)

> **최종 수정일:** 2026-01-13  
> **버전:** v1.2

Prehistoric Life MVP 프론트엔드 설계 문서입니다.

**기술 스택:** React 18 + Phaser 3 + Vite + Zustand + TypeScript

---

## 📋 목차

1. [기술 스택 상세](#1-기술-스택-상세)
2. [프로젝트 구조](#2-프로젝트-구조)
3. [화면 구성](#3-화면-구성)
4. [전투 화면 상세](#4-전투-화면-상세)
5. [UI 컴포넌트](#5-ui-컴포넌트)
6. [상태 관리](#6-상태-관리)
7. [Phaser 게임 씬](#7-phaser-게임-씬)
8. [API 연동](#8-api-연동)
9. [반응형 처리](#9-반응형-처리)

---

## 1. 기술 스택 상세

### 1.1 핵심 라이브러리

| 라이브러리 | 버전 | 용도 |
|------------|------|------|
| **React** | 18.x | UI 컴포넌트 |
| **Phaser** | 3.x | 게임 렌더링 (전투, 애니메이션) |
| **Vite** | 5.x | 빌드 도구 |
| **Zustand** | 4.x | 전역 상태 관리 |
| **React Router** | 6.x | 라우팅 |
| **Axios** | 1.x | HTTP 클라이언트 |
| **Socket.io Client** | 4.x | 실시간 통신 |
| **Tailwind CSS** | 3.x | 스타일링 |

### 1.2 개발 도구

| 도구 | 용도 |
|------|------|
| TypeScript | 타입 안정성 |
| ESLint | 코드 품질 |
| Prettier | 코드 포맷팅 |

---

## 2. 프로젝트 구조

```
/src
├── /assets                 # 정적 에셋
│   ├── /images
│   ├── /sprites
│   └── /audio
│
├── /components             # React 컴포넌트
│   ├── /common             # 공통 (Button, Modal, HealthBar...)
│   ├── /battle             # 전투 UI (BattleHUD, ActionMenu...)
│   ├── /town               # 마을 UI (TownMenu, ShopPanel...)
│   └── /character          # 캐릭터 (CharacterCreate, StatsPanel...)
│
├── /game                   # Phaser 게임 로직
│   ├── /scenes             # BootScene, PreloadScene, BattleScene
│   ├── /entities           # Character, Pet, Monster
│   ├── /systems            # BattleSystem, TurnManager, DamageCalculator
│   └── GameConfig.ts
│
├── /hooks                  # 커스텀 훅 (useAuth, useBattle, useSocket)
├── /pages                  # 페이지 컴포넌트
├── /services               # API 서비스
├── /stores                 # Zustand 스토어
├── /types                  # TypeScript 타입 정의
├── /utils                  # 유틸리티 함수
│
├── App.tsx
├── main.tsx
└── index.css
```

---

## 3. 화면 구성

### 3.1 화면 플로우

```
[로그인] → [캐릭터 선택/생성] → [마을] → [스테이지 선택] → [전투] → [결과] → [마을]
                                  ↓
                            [상점/대장장이/펫관리소]
```

### 3.2 페이지 목록

| 페이지 | 경로 | 설명 |
|--------|------|------|
| 로그인 | `/login` | 이메일/비밀번호 로그인 |
| 회원가입 | `/register` | 계정 생성 |
| 캐릭터 생성 | `/character/create` | 외형, 속성, 스탯 설정 |
| 마을 | `/town` | 메인 허브 |
| 상점 | `/town/shop` | 아이템 구매/판매 |
| 대장장이 | `/town/blacksmith` | 제작/수리 |
| 펫 관리소 | `/town/pet-storage` | 펫 보관/교체 |
| 스테이지 선택 | `/stage` | 맵 형태 스테이지 선택 |
| 요일 던전 | `/daily-dungeon` | 요일별 던전 |
| 전투 | `/battle/:stageId` | 전투 화면 |
| 전투 결과 | `/battle/result` | 승패, 보상 표시 |
| 인벤토리 | `/inventory` | 아이템/장비 관리 |
| 캐릭터 정보 | `/character` | 스탯, 장비 확인 |

### 3.3 마을 화면

```
┌─────────────────────────────────────────────────────────┐
│  [캐릭터 정보]                           [설정] [로그아웃] │
│   Lv.15 닉네임                                          │
│   HP ████████░░  MP ██████░░░░                         │
│   Gold: 12,500                                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│                    🏠 마을 화면                          │
│                   (배경 이미지)                          │
│                                                         │
│   ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐              │
│   │ 상점  │  │대장장이│  │펫관리소│  │ 인벤 │              │
│   └──────┘  └──────┘  └──────┘  └──────┘              │
│                                                         │
│              ┌────────────────────┐                     │
│              │   🗺️ 모험 떠나기    │                     │
│              └────────────────────┘                     │
└─────────────────────────────────────────────────────────┘
```

### 3.4 스테이지 선택 화면 (맵형)

```
┌─────────────────────────────────────────────────────────┐
│  [뒤로가기]        스테이지 선택           [요일 던전]    │
├─────────────────────────────────────────────────────────┤
│                                                         │
│     ○───○───○───○───●                                  │
│     1   2   3   4   5(보스)                             │
│                     │                                   │
│     ○───○───○───○───○                                  │
│     6   7   8   9   10(보스)                            │
│                     │                                   │
│     🔒──🔒──🔒──🔒──🔒                                  │
│     11  12  13  14  15                                 │
│                                                         │
│  ○ = 클리어    ● = 현재    🔒 = 잠김                     │
│                                                         │
├─────────────────────────────────────────────────────────┤
│  [선택된 스테이지 정보]                                   │
│  스테이지 5 - 초원의 우두머리 ⭐⭐☆                       │
│  권장 레벨: Lv.5    보상: 120 EXP, 50 Gold              │
│                                                         │
│              ┌────────────────┐                         │
│              │    입장하기     │                         │
│              └────────────────┘                         │
└─────────────────────────────────────────────────────────┘
```

---

## 4. 전투 화면 상세

### 4.1 전투 화면 레이아웃

```
┌─────────────────────────────────────────────────────────────────┐
│ ┌──────────┐              ┌──────────┐         ┌─────────────┐ │
│ │ Stage    │              │  ⏱️ 30   │         │ ⚔️ 🛡️ ✨ 🎒 │ │
│ │ 1-5 보스  │              │ (타이머)  │         │ 🎯 🏃      │ │
│ └──────────┘              └──────────┘         └─────────────┘ │
│  좌측상단                    중앙상단              우측상단       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│                      [Phaser 게임 캔버스]                        │
│                                                                 │
│    ┌─────┐                              ┌─────┐                │
│    │HP/MP│                              │HP/MP│                │
│    │ 적1 │ ┌─────┐ ┌─────┐   ┌─────┐   │펫1 │ ┌─────┐        │
│    └─────┘ │ 적2 │ │ 적3 │   │ 펫2 │   └─────┘ │펫3 │        │
│            └─────┘ └─────┘   └─────┘           └─────┘        │
│                                          ┌─────┐              │
│                                          │캐릭터│              │
│                                          └─────┘              │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  [턴 정보: 내 턴 - 캐릭터]        [전투 로그 (최근 3줄)]          │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 UI 요소 상세

#### 좌측 상단 - 스테이지 정보

```tsx
interface StageInfoProps {
  chapter: number;      // 챕터 (1, 2, 3...)
  stageNumber: number;  // 스테이지 번호 (1-10)
  stageName: string;    // "초원의 우두머리"
  stageType: 'normal' | 'wave' | 'boss';
  currentWave?: number; // 웨이브 스테이지일 경우
  totalWaves?: number;
}
```

#### 중앙 상단 - 타이머

```tsx
interface TurnTimerProps {
  timeRemaining: number;  // 남은 시간 (초)
  maxTime: number;        // 최대 시간 (30초)
  isMyTurn: boolean;      // 내 턴인지
  currentUnit: string;    // "캐릭터" | "펫1" | "펫2" | "펫3"
}
```

| 타이머 상태 | 색상 | 동작 |
|-------------|------|------|
| 30~11초 | 흰색 | 정상 |
| 10~4초 | 노란색 | 경고 |
| 3~0초 | 빨간색 + 깜빡임 | 긴급 |
| 타임오버 | - | **대기 상태** (공격/방어 안함) |

#### 우측 상단 - 액션 아이콘

```
┌────┬────┬────┐
│ ⚔️ │ 🛡️ │ ✨ │   ⚔️ 공격   🛡️ 방어   ✨ 주술
├────┼────┼────┤
│ 🎒 │ 🎯 │ 🏃 │   🎒 아이템  🎯 포획   🏃 도망
└────┴────┴────┘
```

| 아이콘 | 동작 | 다음 단계 |
|--------|------|-----------|
| ⚔️ 공격 | **캐릭터**: 바로 타겟 선택 | → 타겟 선택 |
| ⚔️ 공격 | **펫**: 스킬 선택 (기술1/기술2) | → 스킬 선택 → 타겟 선택 |
| 🛡️ 방어 | 바로 적용 | 턴 종료 |
| ✨ 주술 | 주술 목록 표시 | → 주술 선택 → 타겟 선택 |
| 🎒 아이템 | 아이템 목록 표시 | → 아이템 선택 → 타겟 선택 |
| 🎯 포획 | 1레벨 펫만 가능 | → 타겟 선택 |
| 🏃 도망 | 확인 모달 | → 확인 → 마을 이동 |

### 4.3 액션 플로우

> **핵심 원칙**: 모든 액션은 **캐릭터 행동 → 펫 설정** 순서로 진행됩니다.  
> 별도의 팝업창 없이 **테두리 하이라이트**로 선택 대상을 표시합니다.

#### 전체 턴 플로우

```
[캐릭터 행동 선택] → [펫1 설정] → [펫2 설정] → [펫3 설정] → [턴 실행]
```

#### 공격 플로우

```
캐릭터: [공격] → [타겟 선택 (적 하이라이트)]
   ↓
펫1: [스킬 선택 (기술1/기술2 하이라이트)] → [타겟 선택]
   ↓
펫2: [스킬 선택] → [타겟 선택]
   ↓
펫3: [스킬 선택] → [타겟 선택]
   ↓
[턴 실행]
```

#### 방어/주술/아이템/포획 플로우

| 액션 | 캐릭터 단계 | 펫 단계 |
|------|-------------|---------|
| 방어 | 즉시 적용 | 스킬 선택 → 타겟 선택 |
| 주술 | 주술 선택 → 타겟 선택 | 스킬 선택 → 타겟 선택 |
| 아이템 | 아이템 선택 → 타겟 선택 | 스킬 선택 → 타겟 선택 |
| 포획 | 1레벨 펫 타겟 선택 | 스킬 선택 → 타겟 선택 |
| 도망 | 확인 → 마을 이동 | - |

#### 선택 UI 방식 (팝업 없음)

```
선택 모드 활성화 시:
- 선택 가능한 항목에 테두리 하이라이트 (노란색 #FBBF24)
- 선택 불가 항목은 어둡게 처리 (opacity 50%)
- 클릭/탭으로 선택 확정
- 우클릭/ESC로 이전 단계로 돌아감
```

### 4.4 HP/MP 바 표시

```
┌──────────────────┐
│ ████████░░░░░░░░ │  ← HP (초록색 #22C55E) - 고정
│ ██████░░░░░░░░░░ │  ← MP (노란색 #EAB308) - 고정
└──────────────────┘
        👤
     [캐릭터]
```

| 바 종류 | 색상 | Hex |
|---------|------|-----|
| HP 바 | 초록색 (고정) | #22C55E |
| MP 바 | 노란색 (고정) | #EAB308 |
| 바 배경 | 어두운 회색 | #374151 |

> **💡 잔량에 따른 색상 변화 없음** - 단순하고 직관적인 UI

### 4.5 타겟 선택 UI

| 선택 단계 | 하이라이트 대상 |
|-----------|----------------|
| 적 타겟 선택 | 생존한 적 몬스터 |
| 아군 타겟 선택 | 캐릭터 + 파티 펫 |
| 주술 선택 | 장착된 주술 슬롯 (MP 충분한 것만) |
| 아이템 선택 | 인벤토리 소모품 |
| 펫 스킬 선택 | 해당 펫의 기술1, 기술2 |
| 포획 타겟 | 1레벨 펫만 (희귀 표시) |

### 4.6 전투 결과 화면

```
┌─────────────────────────────────────────────────────────┐
│                    🎉 승리! 🎉                          │
│                   ⭐ ⭐ ☆                               │
│                  별 2개 획득                            │
├─────────────────────────────────────────────────────────┤
│   경험치: +150 EXP                                      │
│   골드: +80 Gold                                        │
│                                                         │
│   ── 드랍 아이템 ──                                     │
│   🦴 뼈다귀1 x3                                         │
│   🪨 돌맹이1 x2                                         │
├─────────────────────────────────────────────────────────┤
│   ┌──────────────┐    ┌──────────────┐                 │
│   │   재도전      │    │  마을로 이동  │                 │
│   └──────────────┘    └──────────────┘                 │
└─────────────────────────────────────────────────────────┘
```

---

## 5. UI 컴포넌트

### 5.1 공통 컴포넌트

| 컴포넌트 | Props | 용도 |
|----------|-------|------|
| `Button` | variant, size, disabled, loading | 범용 버튼 |
| `Modal` | isOpen, onClose, title | 모달 창 |
| `HealthBar` | current, max, type, size | HP/MP/EXP 바 |
| `IconButton` | icon, onClick, disabled, tooltip | 아이콘 버튼 |

### 5.2 전투 컴포넌트

| 컴포넌트 | 용도 |
|----------|------|
| `BattleHUD` | 전투 화면 전체 UI 오버레이 |
| `ActionMenu` | 우측 상단 액션 아이콘 메뉴 |
| `SkillSelector` | 펫 스킬 선택 (펫 근처 인라인 표시) |
| `TargetSelector` | 타겟 선택 하이라이트 관리 |
| `TurnInputIndicator` | 현재 입력 단계 표시 |

### 5.3 마을 컴포넌트

| 컴포넌트 | 용도 |
|----------|------|
| `TownMenu` | 마을 메인 메뉴 버튼들 |
| `ShopPanel` | 상점 인터페이스 |
| `BlacksmithPanel` | 제작/수리 인터페이스 |
| `PetStoragePanel` | 펫 관리소 인터페이스 |

---

## 6. 상태 관리

### 6.1 Zustand 스토어 구조

| 스토어 | 관리 대상 |
|--------|-----------|
| `authStore` | 인증 상태 (user, token, isAuthenticated) |
| `characterStore` | 캐릭터 정보 (character, pets, party, equipment) |
| `battleStore` | 전투 상태 (phase, allies, enemies, turnOrder, actions) |
| `inventoryStore` | 인벤토리 (consumables, materials, gold) |

### 6.2 battleStore 상세

```tsx
interface BattleState {
  // 전투 상태
  battleId: string | null;
  phase: 'preparing' | 'in_progress' | 'victory' | 'defeat' | 'fled';
  
  // 유닛 상태
  allies: BattleUnit[];
  enemies: BattleUnit[];
  
  // 턴 상태
  turnOrder: string[];
  currentTurnIndex: number;
  turnTimer: number;
  
  // 행동 입력 상태 (캐릭터 → 펫 순차)
  inputPhase: 'character' | 'pet1' | 'pet2' | 'pet3' | 'ready';
  characterAction: CharacterAction;
  petActions: PetAction[];
  
  // UI 상태
  selectionMode: SelectionMode;
  highlightedTargets: string[];
}
```

### 6.3 스토어 간 연동

```
┌─────────────┐     ┌─────────────┐
│  authStore  │────▶│characterStore│
└─────────────┘     └─────────────┘
                           │
                           ▼
┌─────────────┐     ┌─────────────┐
│ battleStore │◀───▶│inventoryStore│
└─────────────┘     └─────────────┘
```

---

## 7. Phaser 게임 씬

### 7.1 씬 구조

| 씬 | 역할 |
|----|------|
| `BootScene` | 초기 설정, 로고 표시 |
| `PreloadScene` | 에셋 로딩 |
| `BattleScene` | 전투 게임플레이 |

### 7.2 유닛 배치 좌표 (기준: 1920 × 1080)

```typescript
const BATTLE_POSITIONS = {
  // 적군 (왼쪽) - 오른쪽을 바라봄 (미러링)
  enemies: [
    { x: 300, y: 450 },   // 적1
    { x: 450, y: 500 },   // 적2
    { x: 350, y: 550 },   // 적3
  ],
  
  // 아군 (오른쪽) - 왼쪽을 바라봄 (기본)
  allies: {
    character: { x: 1500, y: 550 },
    pets: [
      { x: 1300, y: 450 },  // 펫1
      { x: 1400, y: 500 },  // 펫2
      { x: 1350, y: 550 },  // 펫3
    ]
  }
};
```

### 7.3 애니메이션 설정

| 애니메이션 | frameRate | repeat |
|------------|-----------|--------|
| idle | 6 | -1 (무한) |
| attack | 12 | 0 |
| hurt | 8 | 0 |
| death | 6 | 0 |

---

## 8. API 연동

### 8.1 Axios 인스턴스

- 기본 URL: `import.meta.env.VITE_API_URL`
- 타임아웃: 10초
- 인터셉터: 토큰 자동 첨부, 401 에러 시 로그아웃

### 8.2 Socket.io 연결

| 이벤트 | 방향 | 설명 |
|--------|------|------|
| `battle:turn_start` | 수신 | 턴 시작 알림 |
| `battle:action_result` | 수신 | 액션 결과 |
| `battle:end` | 수신 | 전투 종료 |
| `battle:action` | 송신 | 전투 액션 전송 |
| `party:member_joined` | 수신 | 파티 멤버 참가 |
| `party:member_left` | 수신 | 파티 멤버 퇴장 |

---

## 9. 반응형 처리

### 9.1 브레이크포인트

| 디바이스 | 너비 | 비율 |
|----------|------|------|
| 모바일 | < 768px | 16:9 |
| 태블릿 | 768 ~ 1024px | 16:9 |
| 데스크탑 | > 1024px | 16:9 |

### 9.2 Phaser 캔버스 스케일링

```typescript
const config: Phaser.Types.Core.GameConfig = {
  width: 1920,
  height: 1080,
  scale: {
    mode: Phaser.Scale.FIT,
    autoCenter: Phaser.Scale.CENTER_BOTH,
  },
};
```

### 9.3 모바일 터치 지원

- 멀티터치 지원: `this.input.addPointer(1)`
- 터치/클릭 통합: `sprite.on('pointerdown', handler)`

---

## 📋 MVP 체크리스트

### 필수 구현

- [ ] 로그인/회원가입 페이지
- [ ] 캐릭터 생성 페이지
- [ ] 마을 화면 (버튼 메뉴)
- [ ] 스테이지 선택 (맵형)
- [ ] 전투 화면 (Phaser)
- [ ] 전투 HUD (타이머, 액션, HP/MP)
- [ ] 전투 결과 화면

### 우선순위 중

- [ ] 상점 UI
- [ ] 대장장이 UI
- [ ] 펫 관리소 UI
- [ ] 인벤토리 UI

### 나중에

- [ ] 요일 던전 UI
- [ ] 멀티플레이 파티 UI
- [ ] 설정 화면

---

## 📝 버전 히스토리

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| v1.0 | 2026-01-13 | 초기 작성 |
| v1.1 | 2026-01-13 | 액션 플로우 수정 (캐릭터→펫 순차), HP/MP 바 색상 고정, 팝업 제거→하이라이트 방식 |
| v1.2 | 2026-01-13 | 문서 포맷 통일, 중복 제거, 테이블 정리 |
